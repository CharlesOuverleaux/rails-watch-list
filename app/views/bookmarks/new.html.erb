<h1>Bookmarks new</h1>


<%= simple_form_for [ @list, @bookmark ] do |f| %>
  <%= f.input :comment %>
  <%= f.association :movie, prompt: 'select the movie' %>
  <%= f.submit "Submit bookmark", class: "btn btn-primary" %>
<% end %>
